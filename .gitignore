node_modules/
dist/
.DS_Store
*.log

# Environment secrets
.env

# SQLite database + WAL files (created at runtime)
server/data/
*.db
*.db-shm
*.db-wal

# Deployment build artifact (rebuilt by deployment.md each time)
*.tar
